{
    "builds": [{
        "src": "time_table/wsgi.py",
        "use": "@vercel/python",
        "config": { "maxLambdaSize": "15mb" }
    }],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "time_table/wsgi.py"
        }
    ]
}